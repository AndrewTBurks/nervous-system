# ThreadWeaver — CNS Notes

## Project Location

`/Users/andrew/Documents/thesis-project/` — git repo, GitHub, arXiv

## .cns/ Structure

```
.cns/
  index.md           # root — ThreadWeaver overview, decisions, status
  intent.md          # all planned work
  log.md             # action log (capture, reconcile, shard, bubble)
  graph.json         # generated by extract.py
  architecture/
    index.md, primitives.md, stubs.md, branching.md,
    provenance.md, orchestration.md, implementation.md
  design/
    index.md         # schema DSL, edge rendering, AI vs human visual distinction
  product/
    index.md         # research prototype, users, study tasks
  research/
    index.md, literature.md
```

## ThreadWeaver Vocabulary (from memory)

- **warp** = data flow
- **weft** = parameter flow
- **draft** = stub
- **hand-placed** = scientist panel
- **warp-born** = AI panel
- **split** = fork

## Architecture (from .cns/index.md)

- Dual dataflow: data + parameter flows
- Schema-validated primitive composition
- Branching as topological operation (no snapshots)
- Orchestrator as primitive
- Append-only event log for provenance
- Toolchain: Bun runtime, `bun test`

## Key Systems

- `src/engine/` — Workflow orchestration (graph, execution, topology, branching, stubs)
- `src/shared/types/graph.ts` — Discriminated union type system (StubNode | PrimitiveNode)
- `src/provenance/` — Append-only event log
- `src/server/` — Backend server + LLM orchestrator
- `src/client/` — React workspace UI
- `src/wall/` — Large display rendering

## Current Status (from .cns/index.md)

Phases 1-4 complete. 128 tests passing across 24 test files. Domain primitives (XPCS + cosmology) implemented. Force-directed layout, spatial intent disambiguation, branch comparison, inline provenance timeline all wired and tested.

## Supervisor

Associated with IDEAS project (HACC cosmology + XPCS at APS).

## Key Decisions

- DEC-001: Initial .cns/ nervous system setup — graph type system refactor
- DEC-002: StubNode/PrimitiveNode split
- DEC-003: validateConnection returns string[] not {valid, errors}
- DEC-016: XPCS and cosmology domain primitives
- DEC-017: Phases 1-4 complete

## Human Notes (from .cns/index.md)

- House on Fire VR project uses Tsoupikova's Design+Theater+Coding methodology
- Andrew prefers direct/honest feedback, rejects clinical language