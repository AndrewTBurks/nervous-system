# Bootstrap Preflight

- Confirm `.cns/` does not already exist.
- If user says "bootstrap" as continuation of prior work, search prior sessions or project artifacts before creating skeleton docs.
- Check Python 3.9+ and PyYAML.
- Check git status.
- Ask for project name/description/stack/modules only if not inferable and materially useful.
